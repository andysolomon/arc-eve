{
  "name": "arc-eve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "info": "eve info",
    "build": "eve build",
    "provider-auth": "tsx scripts/provider-auth.ts",
    "test": "node --test --import tsx test/**/*.test.ts"
  },
  "dependencies": { "eve": "0.54.3", "zod": "^4.0.0" },
  "devDependencies": { "@types/node": "^24.0.0", "tsx": "^4.19.0", "typescript": "^5.9.0" }
}
